/*
name: sketch.js
author: Miya Fordah

*/

// This is an in-line comment.
// Here's another one.

// This is the setup() fuction definition
// This gets called after setup() finishes executing
function setup(){
}

// This is the draw() fuction definition
// This gets called after setup() finishes executing
function draw(){

  // This will draw a circle
  // Centered at point (20,20)
  // It will have a diameter of 30 pixels
  ellipse( 20, 20, 30, 30 ); //everything after these slashes is ignored
}
